#ifndef HEADER_H
#define HEADER_H

#define _GNU_SOURCE

#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>

int isPrime(int number);
int printer(char *buf);
int rsa(char *buf);

#endif /* HEADER_H */
